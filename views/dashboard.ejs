<%- include("partials/header") %>

    <h1>Welcome, <%= user.name %>!</h1>

    <% if (!hasData) { %>
        <button id="connectBtn" class="connect-btn">Connect Your Bank</button>
        <% } else { %>

            <div class="orb-container">
                <div class="orb"></div>
            </div>

            <h2>Your Financial Snapshot</h2>

            <p>Breathing Room Score: <%= orbScore %>/100</p>
            <p>Essentials Used: <%= essentialsPct %>% of income</p>
            <p>Main Pressure: <%= mainPressure %>
            </p>
            <p>Runway: <%= runway %> days</p>

            <% } %>

                <script src="https://cdn.plaid.com/link/v2/stable/link-initialize.js"></script>

                <script src="/js/dashboard.js"></script>

                <%- include("partials/footer") %>